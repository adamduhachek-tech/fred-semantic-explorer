# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# local dev server log + locally-downloaded source data
.devserver.log
/data/
# NB: /public/data/ (the precomputed corpus bundle) is intentionally COMMITTED
# so a fresh clone / deploy is self-contained — see .gitattributes for the .bin.

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
